#ifndef PARSING_H
#define PARSING_H
#include <string>
#include <bits/stdc++.h>
#include <iostream>
#include <cstring>

using namespace std;

class Parsing
{
    public:
        Parsing();
        void parse(char *input);
    protected:

    private:
};

#endif // PARSING_H
